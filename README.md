# my-weekly-update

kalirali 每周实习周报静态页，使用 GitHub Pages 托管。

## 更新方式

1. 将新一周图片放入 `assets/`。
2. 在 `index.html` 中复制一个 `<article class="report">` 周报块，修改 `id`、标题、周期、目录锚点和内容。
3. 在顶部切换按钮、左侧周报目录和锚点目录里补上新周报入口。
4. 提交并推送到 `main` 分支，GitHub Pages 会自动更新。
